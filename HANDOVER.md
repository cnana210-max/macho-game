# 引き継ぎメモ（マッチョ育成ゲーム）

## このゲームはどんなゲームか
「マッチョ育成ゲーム」は、キャラクターの体の部位（胸・腕・脚・肩・背中・腹）をタップして鍛える育成ゲームです。タップすると筋トレ回数と筋力Pがたまり、部位の正式名称と説明が表示されるので、遊びながら部位名を覚えられます。

## 公開URL
現時点で確認できている遊べるリンクは、このプロジェクトで使っているClaudeのプレビュー（Artifact）です。
- https://claude.ai/artifact/TwUmXUdzCWZAbakqU8tvFb

CLAUDE.mdには「GitHub Pagesで誰でも遊べるように公開する」という方針が書かれていますが、GitHub Pagesが実際に有効になっているかはこのセッションからは確認できていません（`gh-pages`ブランチなどの設定は見当たりませんでした）。正式に公開したい場合は、リポジトリの Settings → Pages での設定が必要か、あらためて確認が必要です。

## 今できていること
- キャラクターの体型が7段階に変化（Lv.1〜Lv.7、筋力Pに応じて自動で進化）
- 筋力P（貯まる数値）と称号（段階ごとの二つ名）の表示
- レベルアップ時の派手な演出（黒背景に映える光の演出）
- 部位をタップした瞬間、その部位が光る演出・背中タップ時の後光演出
- ジム（ショップ）：プロテイン・ダンベルなど6種類のアイテムを購入でき、自動で筋力Pが増える
- 筋肉クイズ：5問のクイズに挑戦し、正解数に応じて筋力Pを獲得
- オフライン報酬：しばらく開いていない間の分を、戻ってきたときに帯（バナー）で受け取れる
- デイリーボーナス：1日1回、連続ログイン日数に応じて筋力Pが増える帯
- 実績バッジ：12種類の条件達成バッジ。獲得済みバッジ数をボタンに表示し、一覧をタップで開閉、獲得時は画面上部に自動で消える帯を表示

## 使っている素材
- `images/lv1.png`〜`images/lv6.png`：ゲーム開発の初期段階で作成したルチャドール（覆面レスラー）風のキャラクター画像
- `images/lv7.png`：隠し体型Lv.7用の画像。開発者から提供された「人外ゴリゴリゴリ超マッチョ」というキャラクター写真をもとに、タイトル文字を取り除き、背景を透過処理して作成したもの

## 技術的な作り
- `index.html` 1ファイルにHTML・CSS・JavaScriptをすべてまとめた構成（ビルド不要）
- 進行状況（筋力P・部位ごとの回数・所持アイテム・実績バッジなど）はブラウザのlocalStorageに保存し、次回開いたときに続きから遊べる
- 外部の画像・音声ファイルへの依存はなく、`images/`フォルダ内の画像とインラインのコード・演出のみで完結

## 過去に起きた不具合と教訓
- **画面を覆うカードを閉じても裏のボタンが押せなくなる不具合**：`[hidden]`属性を付けても、同じ要素に`display`を指定するCSSがあるとそちらが優先されてしまい、見た目は消えても操作をブロックし続けることがあった。以降、モーダルやバナーを追加するときは必ず`.要素名[hidden] { display: none; }`を明示し、閉じたあとに他のボタンがすべて押せることを確認してから保存するようにしている
- **部位が光る位置のずれ**：キャラクターの体型が段階によって変わるため、1つの座標をすべての段階で使い回すとズレが出た。段階ごとに画像を実測し、座標表（`PART_GLOW_POINTS_BY_STAGE`）を段階別に持つ形に直した。新しい段階を追加するときも、必ず画像を実測してから座標を決めている
- **会話の要約で別プロジェクトの内容が混ざった件**：長い会話が要約された際に、別のリポジトリ（サイドレイズファイト、識別記号A）の作業内容がこのプロジェクトの続きであるかのように混ざって渡ってきたことがあった。実際にはリポジトリも中身も別物だった。以降、作業を再開するときはGitHubの実際の状態（リポジトリ名・ブランチ・コミット履歴）を必ず確認してから進めるようにしている

## やり残し・今後の候補
- ロードマップの項目10「刺繍ワッペン風の全体デザイン仕上げ」は未着手（ボタンや背景など、キャラクター以外の部分にも刺繍ワッペン風の質感を足す案）
- さらに上の隠し体型（Lv.8以降）は、新しいキャラクター画像をもらえれば同じ手順で追加可能
- 効果音の追加（タップ音・実績解除音など。外部ファイルなしでコードのみで実装可能）
- GitHub Pagesでの正式公開の確認・設定
