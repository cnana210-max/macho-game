# プロジェクト：マッチョ育成ゲーム（識別記号：B）

## このアプリについて
- 「タップで育成」＋「筋肉の部位を覚える学習」を合わせたブラウザゲーム
- 部位ボタンをタップすると筋トレ回数がたまり、その部位が太くなる。同時に正式名称と短い説明が表示され、遊ぶうちに部位名を覚えられる
- スマホ・タブレットで遊べるウェブアプリ。GitHub Pagesで誰でも遊べるように公開する
- 技術構成は「HTML1枚（index.html）にすべて入れる」形を基本とする。ビルド作業や環境構築が不要な形を最優先する
- 進行状況はブラウザのlocalStorageに保存し、次に開いたとき続きから遊べるようにする

## 開発者について
- 開発者はコードが読めない非エンジニアで、iPadのみを使用する（パソコン・ターミナルは使えない）
- 返答は日本語。専門用語は避けるか一言で説明する
- 開発者の操作は「コードブロックのコピーボタンを押す」「貼り付ける」「画面のボタンをタップする」「音声で話す」だけ。文字を打つことや、長い文章から必要な部分を探すことはしない

## レポートを出す前の必須チェック（例外なし）
1. レポート本文を1つのコードブロックで出したか
2. プレビューURLを、URLだけの別のコードブロックで出したか
3. ★付きの番号選択肢を出したか
※見た目の変更がない回でも、3つすべて必ず出す
※タップ式の質問機能が使えない・拒否された場合は、その場で★付き番号に切り替える
※「前回と同じ」「上記参照」など、別の場所を指す書き方は禁止。プレビューURLも確認ポイントも、毎回その回のレポート内に書き直す

## レポートの形（毎回この順番）
- 本文（1つのコードブロック）：【B-通し番号】／うまくいった点／気になった点（なければ「なし」）／遊んで確認するポイント（どの画面で、どこをタップして、何を見るか）／保存状況
- その下に、プレビューURLだけのコードブロック
- 最後に★付きの番号選択肢

## git操作（すべて自動）
- ブランチ作成・コミット・プッシュ・プルリクエスト作成・マージは、すべてあなたが行い、開発者に操作を求めない
- 作業は毎回mainの最新から、claude/ で始まる新しいブランチで行う
- 開発者が「OK・次へ」の番号を返したら、mainへマージし、そのまま次の作業を始める
- 「元に戻す」を選ばれたら、そのプルリクエストはマージせず閉じる
- 自動でできない操作があるときだけ、理由を一言で伝え、開発者が押すボタン名をそのまま書く
- 新しい作業を始める前に、前の作業がmainに保存済みか必ず確認する

## 作業の進め方（過去の失敗から学んだ鉄則）
- 1回の作業で変えるのは1〜3個まで
- 見た目・デザインの変更は1回1項目だけ。複数まとめて変えると必ず崩れる
- 位置やレイアウトの基準を変えるときは、1か所ずつ直し、すべての画面で確認してから次に進む
- 変更のたびにプレビューを更新し、開発者が確認できるようにする
- 開発者が「おかしい」と言ったら、直す前に「崩れる前の状態に戻す」を選択肢に入れる
- 指示にないこと（判定、スコア、動作の流れなど）は変えない
- 開発者はスクリーンショットで状況を伝えるので、それをよく見て判断する

## 相談役としての進め方
- 開発者は、やりたいことを音声で直接話す（聞き間違いや誤字は文脈から読み取る）
- 開発者に指示文を作らせない。要望を聞いたら、あなたが計画を立ててそのまま作業する
- 大きな要望は小さなステップに分け、下の「ロードマップ」に書いて1つずつ進める
- あいまいな要望は、あなたが解釈して具体案にし、必要なら★付き選択肢で確認する
- アイデアを出すときは、難しさを「かんたん／ふつう／むずかしい」で添える
- 節目ごとに HANDOVER.md（何ができているか、やり残しは何か）を更新する

## ロードマップ
1. 【完了】部位3つ（胸・腕・脚）のタップ育成
2. 【完了】進行状況の保存・最初からボタン
3. 【今ここ】部位「腹」を追加
4. キャラクターをルチャリブレ（覆面レスラー）のデザインに変更。マスクを被った男が、スリム体型からタップに応じて段階的にマッチョになる
5. クイズモード（この部位の名前は？を4択で出題）
6. レベル・称号（ガリ→ふつう→マッチョ→王）
7. デザイン仕上げ・共有しやすくする
